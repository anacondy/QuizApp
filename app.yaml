runtime: python39

entrypoint: gunicorn -b :$PORT app:app

env_variables:
  FLASK_ENV: "production"
  # Add your GEMINI_API_KEY in Google Cloud Console Environment Variables

instance_class: F2

automatic_scaling:
  min_instances: 1
  max_instances: 5
